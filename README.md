# Class App's dev challenge

* Developed with NodeJs version 8.9.4;
* Uses fast-csv library for reading CSV;
* Uses google-libphonenumber for validation phone numbers.

To execute, just run app.js with NodeJS!
